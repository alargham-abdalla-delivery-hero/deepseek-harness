## 1. Resolve open design questions before writing code

- [x] 1.1 **Decided**: non-trivial = the parsed tree contains at least one `Card`/`Table`/`List`/`BarChart`/`PieChart` anywhere (`hasNonTrivialComponent`, a recursive walk over `ElementNode.props`); a bare `Stack`/`Heading`/`Text`-only root does not match. Covered by a dedicated test.
- [x] 1.2 **Decided**: `Config.maxCorrectionAttempts` (default 2), a per-agent in-memory `WeakMap<Agent, number>` counter reset once a turn closes without triggering a correction — not durable, since corrections are expected to be rare and losing the count across a process restart only costs one extra retry, not a correctness issue.
- [x] 1.3 **Resolved: no `inject` growth needed.** Verified against `packages/hooks/hooks-claude-code/src/index.ts` (`export const inject = ['shell']`, no `agent`-capability injection) that `agent/turn-stopping` is reached via plain `ctx.on(...)` and its payload carries the `Agent` instance directly; `agent.steer`/`createUserMessage` signatures confirmed against `packages/core/agent/src/runtime-types.ts` and `packages/llm/llm/src/message.ts`.

## 2. Correct the system-prompt preamble and add a worked example

- [x] 2.0 **Primary fix. Done.** Verified: calling `promptText()` with no options (today's exact call at `tool-openui/src/index.ts:63`) injects `@openuidev/lang-core`'s default `preamble` verbatim — "Your ENTIRE response must be valid openui-lang code — no markdown, no explanations, just openui-lang." Author a replacement preamble string stating that OpenUI Lang is sent only as the `render_ui` tool's `source` argument, never as the model's direct reply, and pass it via `promptText({ preamble: ... })`.
- [x] 2.1 Author one small, clearly-illustrative OpenUI Lang source example (e.g. a `Stack` containing a `Card` with a `Heading` and a `Table`) distinct from any plausible real user request.
- [x] 2.2 Pass it through the same `promptText(options)` call as `examples: [example]` — verified: `examples` renders unconditionally into a `## Examples` section regardless of `toolCalls`/`tools` (confirmed by reading `generatePrompt()`'s source directly), so it adds no unrelated Query/Mutation/tool-workflow sections. Do NOT use `toolExamples` — that field is documented as "Examples shown when tools ARE present (Query/Mutation patterns)," which is out of scope (design.md Non-Goals).
- [x] 2.3 Unit test: `promptText()`'s output no longer contains the raw-completion instruction ("ENTIRE response must be valid openui-lang code"); it does contain the corrected preamble and the worked example; the example itself round-trips through `parseSource()` to a clean, non-null, zero-error root (the taught example must itself be valid, or the model is being taught a broken pattern).

## 3. Turn-completion self-correction listener (inside the existing `dsh-tool-openui` plugin — no new plugin package)

- [x] 3.1 Registered the listener from `dsh-tool-openui`'s existing `apply(ctx, config)` (`packages/openui/tool-openui/src/index.ts`), alongside the existing `render_ui` tool and `tool:render_ui` system-prompt section — not from a new package. No new `inject` entry was needed.
- [x] 3.2 Registered an `agent/turn-stopping` listener in `apply(ctx, config)` that reads the LAST derived message via `agent.session.deriveMessages()`, bails out unless it is role `'assistant'`, and finds text blocks whose content parses via `dsh-openui-lang`'s `parseSource()` to the match condition from task 1.1. Confirmed against the real agent loop (driven through `dsh-agent-loop-testkit`) that a `render_ui` tool-call check on the same message is unreachable dead code and correctly left out.
- [x] 3.3 On a text-only match, calls `agent.steer(createUserMessage({ content: [...], source: { kind: 'plugin', plugin: 'tool-openui', form: 'notice', summary: '...' } }))` with a corrective instruction naming the mistake and telling the model to resend the content via `render_ui`.
- [x] 3.4 Implemented the correction cap from task 1.2: tracks consecutive corrections per agent and stops steering once the cap is reached, letting the turn's text stand.
- [x] 3.5 Package invariant: confirmed no new runtime-invariant entry is needed — the listener introduces no new durable event type, only an ordinary `agent.steer()`-driven `user/message` event already owned by agent-loop/session infrastructure. `invariant.ts`'s justification comment updated to say so explicitly; `verify-package-invariants` passes.
- [x] 3.6 Unit tests (19 total, 100% per-file coverage): text-only OpenUI Lang triggers exactly one steer call and the model recovers via `render_ui`; a mid-turn message with a pending tool call is never inspected; prose that fails to parse cleanly, and a trivial `Text`/`Heading`-only root, never trigger a steer; an empty max-tokens step (no trailing assistant message) is handled without steering; the cap suppresses steering after the configured count; disposing the plugin fiber removes the listener (HMR-safety); an invalid `maxCorrectionAttempts` fails loud at plugin load.

## 4. Documentation and verification

- [x] 4.1 Update `dsh-tool-openui`'s README: Model Experience (the worked example's token effect; the corrective-turn's token/KV-cache effect as an appended, non-prefix-breaking message) and Known Limitations (the false-positive risk from design.md, the correction cap and its current value).
- [ ] 4.2 **Not done, scoped and deferred on evidence, not skipped.** Investigated the concrete cost: `examples/acp-agent/cordis.yml` (the ACP snapshot host) composes its own explicit plugin list and does not register `tool-openui` today, so a `render_ui` snapshot fixture is not "just a fixture" — it requires registering `tool-openui` (+ its `dsh-openui-lang` dependency) in that shared example, which contributes a new system-prompt section that several OTHER pinned scenarios' `system-prompt.expected.md` snapshot verbatim; adding it would require auditing and likely regenerating those unrelated fixtures too. That blast radius is out of proportion to this change's scope (a reliability fix inside one already-registered tool, not a snapshot-infrastructure change) and risks silently perturbing unrelated coverage. Authoring the fixture itself is otherwise cheap and keyless (an authored, `recorded: false` `session.jsonl` — confirmed no live API call is needed, following the `pty-tools`/`read-image-dimension` precedent in `examples/acp-agent/tests/snapshots/`) — this remains the same gap `openui-generative-output` tasks.md 5.3 already left open, now with a concrete cost estimate for whoever picks it up: ~4 files (`input.json`, `session.jsonl`, `stdout.expected.jsonl`, one `SCENARIOS` table row) per case, plus the `tool-openui` wiring change and its fixture-audit fallout.
- [x] 4.3 Ran scoped checks on the touched files only: `tsc --build` for `dsh-tool-openui`'s project reference, `run-oxlint` scoped to changed files, `vitest run --coverage` for the package (100% per-file), `verify-package-invariants`, `verify-cordis-config`, and both package-README doc-sync gates — all pass. `constraints`/`verify-tool-catalog` were also run and show pre-existing failures in unrelated packages (`ui-quick-actions`, `tool-present-artifact`, others) untouched by this change.
- [x] 4.4 Wrote `.agents/notes/implemented/bug-fix/2026-08-30-openui-render-ui-tool-call-reliability.md`, cross-linked to the original note, plus its `.zh.md` bilingual counterpart (required once `verify-translation-pairing` started enforcing it after merging `origin/master`/`savvio/master`, which also surfaced the same gap on the three openui package READMEs — fixed alongside).
